name: 🕵️‍♂️ Pedir Informe Privado

on:
  workflow_dispatch:  # Esto crea el botón manual

jobs:
  enviar-informe:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v3

      - name: Instalar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Instalar librerías
        run: pip install python-dotenv requests

      - name: GENERAR INFORME
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          ADMIN_ID: ${{ secrets.ADMIN_ID }}
        # TRUCO: Importamos el bot pero solo ejecutamos la función de informe
        run: python -c "from bot import enviar_informe_semanal; enviar_informe_semanal()"